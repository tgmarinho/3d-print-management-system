# Fundação (bootstrap + auth + schema + audit log) — SPEC de Implementação

> **Altura: técnica.** Como construir. Assume um agente sem contexto do codebase.
> Volátil — descartável após o merge. Deriva de um PRD em `docs/prd/`. Skill:
> `writing-plans`. Executar com `subagent-driven-development` ou `executing-plans`.
>
> **Para workers agênticos:** SUB-SKILL OBRIGATÓRIA — use
> `subagent-driven-development` (recomendado) ou `executing-plans` para implementar
> tarefa a tarefa. Os passos usam checkbox (`- [ ]`) para rastreio.

**PRD de origem:** [docs/prd/001-gestao-impressao-3d.md](../prd/001-gestao-impressao-3d.md)
**Goal:** Levantar o projeto Next.js + Supabase com autenticação funcionando, o
schema completo do banco (todas as tabelas + RLS + Realtime), o helper de log de
auditoria e o shell mobile-first protegido — base sobre a qual os módulos rodam.
**Architecture:** Next.js (App Router) + TypeScript estrito na Vercel; Supabase
(Postgres + Auth + Realtime) com `@supabase/ssr` para sessões cookie-based. Todo
o schema vive em migrations versionadas (`supabase/migrations/`). RLS libera tudo
para qualquer usuário autenticado (sem RBAC, por decisão de produto). Auditoria
por helper server-side único.
**Tech Stack:** Bun, Next.js 16, TypeScript, Tailwind CSS, shadcn/ui,
`@supabase/ssr`, `@supabase/supabase-js`, Supabase CLI, `bun test`.

---

## Roadmap de SPECs (o PRD inteiro)

Este PRD gera 5 SPECs. Cada um produz software funcional e testável. **Este
documento é o SPEC 1.** Os demais serão escritos depois, derivando deste schema.

| # | SPEC | Cobre (histórias do PRD) |
| --- | --- | --- |
| **1** | **Fundação** (este) | Auth (1–2), schema de tudo, RLS, Realtime, audit log infra (28), app shell |
| 2 | Cadastros | Clientes (3–4), Vendedores/Modeladores (5–7), Produtos (8–9), Locais (14) |
| 3 | Estoque de filamentos | Filamentos + estoque por local + ajuste rápido + Realtime + estoque baixo (10–16) |
| 4 | Pedidos & operação | Orçamentos (17–20), Produção (21–22), Fila (23–24), Pagamento (25–26) |
| 5 | Dashboard & auditoria | Dashboard visual (27), consulta do histórico (29) |

> O **schema completo** (todas as tabelas) é entregue aqui, no SPEC 1, porque é
> infraestrutura compartilhada. Os SPECs 2–5 só constroem UI, server actions e
> Realtime sobre tabelas que já existem.

---

## Estrutura de arquivos

Uma responsabilidade por arquivo; o que muda junto vive junto.

| Arquivo | Responsabilidade |
| --- | --- |
| `package.json` | Scripts (`dev`, `build`, `lint`, `test`) e dependências (Bun) |
| `tsconfig.json` | TypeScript estrito (`strict: true`, sem `any` implícito) |
| `.env.local.example` | Documenta as variáveis de ambiente necessárias (sem segredos) |
| `.gitignore` | Ignora `.env*`, `.next`, `node_modules`, `.context/` |
| `supabase/config.toml` | Config do projeto Supabase local (gerado pelo CLI) |
| `supabase/migrations/0001_schema.sql` | DDL de todas as tabelas do domínio |
| `supabase/migrations/0002_rls.sql` | RLS: acesso total para `authenticated` |
| `supabase/migrations/0003_indexes_realtime.sql` | Índices + publicação Realtime |
| `src/lib/env.ts` | Lê/valida variáveis de ambiente (falha cedo se faltar) |
| `src/lib/supabase/client.ts` | Cliente Supabase para componentes de browser |
| `src/lib/supabase/server.ts` | Cliente Supabase server-side (cookies) |
| `src/lib/supabase/middleware.ts` | Refresh de sessão + guarda de rota |
| `src/lib/audit.ts` | `buildAuditEntry()` (puro) + `logAudit()` (escreve no banco) |
| `src/middleware.ts` | Entrada do middleware do Next (delega ao supabase/middleware) |
| `src/app/layout.tsx` | Layout raiz (html/body, fontes, Tailwind) |
| `src/app/(auth)/login/page.tsx` | Página de login + signup |
| `src/app/(auth)/login/actions.ts` | Server actions `signIn` / `signUp` / `signOut` |
| `src/app/(app)/layout.tsx` | Shell autenticado: header + bottom nav mobile-first |
| `src/app/(app)/dashboard/page.tsx` | Placeholder do dashboard (rota protegida) |
| `src/components/bottom-nav.tsx` | Navegação inferior (mobile-first) |
| `src/lib/audit.test.ts` | Testes do `buildAuditEntry` |
| `src/lib/env.test.ts` | Testes do parser de env |

---

## Pré-requisitos

- Bun instalado (`bun --version`).
- Supabase CLI instalado (`bunx supabase --version` ou `brew install supabase/tap/supabase`).
- Docker em execução (o `supabase start` sobe Postgres local).

---

## Tarefas

Cada passo é uma ação (2–5 min), em ordem TDD onde aplicável.

### Tarefa 1: Bootstrap do projeto Next.js com Bun

**Files:**
- Create: projeto Next.js (gera `package.json`, `tsconfig.json`, `src/app/…`)

- [ ] **Passo 1: Criar o app na raiz do workspace**

Run:
```bash
bun create next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-eslint --use-bun --yes
```
Expected: estrutura `src/app/` criada; `bun.lock` presente. Se o diretório não
estiver vazio, mova `docs/`, `AGENTS.md`, `CLAUDE.md`, `README.md` para um temp,
rode o comando e volte os arquivos (o `create-next-app` exige diretório limpo).

- [ ] **Passo 2: Garantir TypeScript estrito**

Edite `tsconfig.json` para conter, em `compilerOptions`:
```json
{
  "strict": true,
  "noUncheckedIndexedAccess": true,
  "noImplicitOverride": true
}
```

- [ ] **Passo 3: Adicionar script de teste**

Em `package.json`, em `"scripts"`, adicione:
```json
"test": "bun test"
```

- [ ] **Passo 4: Verificar build e dev**

Run: `bun install && bun run build`
Expected: build conclui sem erros.

- [ ] **Passo 5: Commit**

```bash
git add -A
git commit -m "chore: bootstrap Next.js + Bun + TypeScript estrito"
```

---

### Tarefa 2: Configurar shadcn/ui

**Files:**
- Create/Modify: `components.json`, `src/lib/utils.ts`, `src/app/globals.css`

> Consulte a skill `shadcn` antes; use o CLI, não copie componentes à mão.

- [ ] **Passo 1: Inicializar shadcn**

Run:
```bash
bunx --bun shadcn@latest init -d
```
Expected: `components.json` criado; `src/lib/utils.ts` com `cn()`.

- [ ] **Passo 2: Instalar componentes base usados na fundação**

Run:
```bash
bunx --bun shadcn@latest add button input label card
```
Expected: arquivos em `src/components/ui/`.

- [ ] **Passo 3: Verificar build**

Run: `bun run build`
Expected: build verde.

- [ ] **Passo 4: Commit**

```bash
git add -A
git commit -m "chore: configurar shadcn/ui + componentes base"
```

---

### Tarefa 3: Variáveis de ambiente e validação

**Files:**
- Create: `.env.local.example`, `src/lib/env.ts`, `src/lib/env.test.ts`

- [ ] **Passo 1: Escrever o teste que falha**

`src/lib/env.test.ts`:
```ts
import { expect, test } from "bun:test";
import { parseEnv } from "./env";

test("parseEnv retorna as URLs/keys quando presentes", () => {
  const result = parseEnv({
    NEXT_PUBLIC_SUPABASE_URL: "https://x.supabase.co",
    NEXT_PUBLIC_SUPABASE_ANON_KEY: "anon-key",
  });
  expect(result.supabaseUrl).toBe("https://x.supabase.co");
  expect(result.supabaseAnonKey).toBe("anon-key");
});

test("parseEnv lança erro quando falta variável", () => {
  expect(() => parseEnv({ NEXT_PUBLIC_SUPABASE_URL: "https://x.supabase.co" })).toThrow(
    /NEXT_PUBLIC_SUPABASE_ANON_KEY/,
  );
});
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `bun test src/lib/env.test.ts`
Expected: FAIL — `parseEnv` não existe.

- [ ] **Passo 3: Implementar `env.ts`**

`src/lib/env.ts`:
```ts
type RawEnv = Record<string, string | undefined>;

export function parseEnv(raw: RawEnv = process.env) {
  const supabaseUrl = raw.NEXT_PUBLIC_SUPABASE_URL;
  const supabaseAnonKey = raw.NEXT_PUBLIC_SUPABASE_ANON_KEY;
  if (!supabaseUrl) throw new Error("Falta NEXT_PUBLIC_SUPABASE_URL");
  if (!supabaseAnonKey) throw new Error("Falta NEXT_PUBLIC_SUPABASE_ANON_KEY");
  return { supabaseUrl, supabaseAnonKey };
}

export const env = parseEnv();
```

- [ ] **Passo 4: Criar `.env.local.example`**

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role: SOMENTE em ambiente server (nunca commitar o valor real)
SUPABASE_SERVICE_ROLE_KEY=
```

- [ ] **Passo 5: Rodar e confirmar verde**

Run: `bun test src/lib/env.test.ts`
Expected: PASS (2 testes).

- [ ] **Passo 6: Commit**

```bash
git add src/lib/env.ts src/lib/env.test.ts .env.local.example
git commit -m "feat: validação de variáveis de ambiente"
```

---

### Tarefa 4: Inicializar Supabase local

**Files:**
- Create: `supabase/config.toml` (gerado)

- [ ] **Passo 1: Inicializar e subir o Supabase local**

Run:
```bash
bunx supabase init
bunx supabase start
```
Expected: imprime `API URL`, `anon key`, `service_role key`, `DB URL`.

- [ ] **Passo 2: Preencher `.env.local`**

Copie `API URL` → `NEXT_PUBLIC_SUPABASE_URL`, `anon key` →
`NEXT_PUBLIC_SUPABASE_ANON_KEY`, `service_role key` →
`SUPABASE_SERVICE_ROLE_KEY` em `.env.local` (não commitar este arquivo).

- [ ] **Passo 3: Confirmar que `.env.local` está ignorado**

Run: `git status --porcelain | grep -c .env.local`
Expected: `0` (não rastreado).

- [ ] **Passo 4: Commit**

```bash
git add supabase/config.toml supabase/.gitignore
git commit -m "chore: inicializar Supabase local"
```

---

### Tarefa 5: Migration — schema do domínio

**Files:**
- Create: `supabase/migrations/0001_schema.sql`

> Decisões do PRD refletidas: filamento com `color`/`material` obrigatórios,
> `brand`/`weight` opcionais, `low_stock_threshold` configurável; estoque por
> local com `in_stock` e `on_order` (rolos); produto é catálogo reutilizável
> (não pertence a cliente) e o pedido pode ter produto ad-hoc; pedido tem
> `payment_status`, `production_status`, `queue_position`; `audit_log` único.

- [ ] **Passo 1: Escrever a migration**

`supabase/migrations/0001_schema.sql`:
```sql
create extension if not exists "pgcrypto";

-- Perfil 1:1 com auth.users (todos administradores; sem RBAC).
create table public.profiles (
  id uuid primary key references auth.users (id) on delete cascade,
  name text not null,
  created_at timestamptz not null default now()
);

create table public.clients (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  company text,
  phone text,
  email text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table public.sellers (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  phone text,
  email text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table public.modelers (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  phone text,
  email text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Catálogo reutilizável; não pertence a cliente.
create table public.products (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  description text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table public.stock_locations (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  created_at timestamptz not null default now()
);

create table public.filaments (
  id uuid primary key default gen_random_uuid(),
  color text not null,
  material text not null,
  brand text,
  weight text,
  low_stock_threshold integer not null default 0
    check (low_stock_threshold >= 0),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Estoque por filamento+local: rolos em estoque e encomendados.
create table public.filament_stock (
  filament_id uuid not null references public.filaments (id) on delete cascade,
  location_id uuid not null references public.stock_locations (id) on delete cascade,
  in_stock integer not null default 0 check (in_stock >= 0),
  on_order integer not null default 0 check (on_order >= 0),
  updated_at timestamptz not null default now(),
  primary key (filament_id, location_id)
);

create type public.production_status as enum ('waiting', 'producing', 'done');
create type public.payment_status as enum ('unpaid', 'paid');

create table public.orders (
  id uuid primary key default gen_random_uuid(),
  client_id uuid not null references public.clients (id) on delete restrict,
  seller_id uuid references public.sellers (id) on delete set null,
  modeler_id uuid references public.modelers (id) on delete set null,
  product_id uuid references public.products (id) on delete set null,
  product_description text, -- preenchido quando o produto é ad-hoc (sem product_id)
  quantity integer not null default 1 check (quantity > 0),
  amount numeric(10, 2) not null default 0 check (amount >= 0),
  payment_status public.payment_status not null default 'unpaid',
  production_status public.production_status not null default 'waiting',
  queue_position integer not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  -- garante que haja produto do catálogo OU descrição ad-hoc
  constraint product_present check (product_id is not null or product_description is not null)
);

-- Log único de auditoria (substitui histórico isolado de estoque).
create table public.audit_log (
  id uuid primary key default gen_random_uuid(),
  actor_id uuid references auth.users (id) on delete set null,
  action text not null,            -- create | update | delete | status | priority | stock | payment
  entity_type text not null,       -- client | order | filament | ...
  entity_id uuid,
  details jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

-- Cria profile automaticamente ao surgir um usuário no Auth.
create function public.handle_new_user()
returns trigger
language plpgsql
security definer set search_path = ''
as $$
begin
  insert into public.profiles (id, name)
  values (new.id, coalesce(new.raw_user_meta_data ->> 'name', new.email));
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
```

- [ ] **Passo 2: Aplicar e verificar**

Run:
```bash
bunx supabase migration up
```
Expected: aplica `0001_schema` sem erro.

- [ ] **Passo 3: Conferir que as tabelas existem**

Run:
```bash
bunx supabase db reset
```
Expected: reset reaplica todas as migrations limpo, sem erro.

- [ ] **Passo 4: Commit**

```bash
git add supabase/migrations/0001_schema.sql
git commit -m "feat: schema do domínio (clientes, filamentos, pedidos, audit)"
```

---

### Tarefa 6: Migration — RLS (acesso total para autenticados)

**Files:**
- Create: `supabase/migrations/0002_rls.sql`

> Decisão de produto: sem RBAC. Qualquer usuário autenticado tem acesso total a
> todas as tabelas. Anônimos não acessam nada.

- [ ] **Passo 1: Escrever a migration**

`supabase/migrations/0002_rls.sql`:
```sql
do $$
declare
  t text;
  tables text[] := array[
    'profiles', 'clients', 'sellers', 'modelers', 'products',
    'stock_locations', 'filaments', 'filament_stock', 'orders', 'audit_log'
  ];
begin
  foreach t in array tables loop
    execute format('alter table public.%I enable row level security;', t);
    execute format(
      'create policy %I on public.%I for all to authenticated using (true) with check (true);',
      t || '_authenticated_all', t
    );
  end loop;
end;
$$;
```

- [ ] **Passo 2: Aplicar**

Run: `bunx supabase db reset`
Expected: reaplica tudo, incluindo `0002_rls`, sem erro.

- [ ] **Passo 3: Confirmar RLS habilitado**

Run:
```bash
bunx supabase db reset && echo "ok"
```
Expected: imprime `ok` (sem falha de política).

- [ ] **Passo 4: Commit**

```bash
git add supabase/migrations/0002_rls.sql
git commit -m "feat: RLS de acesso total para usuários autenticados"
```

---

### Tarefa 7: Migration — índices e Realtime

**Files:**
- Create: `supabase/migrations/0003_indexes_realtime.sql`

- [ ] **Passo 1: Escrever a migration**

`supabase/migrations/0003_indexes_realtime.sql`:
```sql
-- Índices para filtros/joins frequentes.
create index orders_client_id_idx on public.orders (client_id);
create index orders_production_status_idx on public.orders (production_status);
create index orders_payment_status_idx on public.orders (payment_status);
create index orders_queue_position_idx on public.orders (queue_position);
create index filament_stock_location_idx on public.filament_stock (location_id);
create index audit_log_created_at_idx on public.audit_log (created_at desc);

-- Realtime: estoque e pedidos atualizam ao vivo entre usuários.
alter publication supabase_realtime add table public.filament_stock;
alter publication supabase_realtime add table public.orders;
```

- [ ] **Passo 2: Aplicar**

Run: `bunx supabase db reset`
Expected: reaplica tudo sem erro.

- [ ] **Passo 3: Commit**

```bash
git add supabase/migrations/0003_indexes_realtime.sql
git commit -m "feat: índices e publicação Realtime (estoque, pedidos)"
```

---

### Tarefa 8: Clientes Supabase (browser e server)

**Files:**
- Create: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`

> Consulte a skill `supabase` / `supabase-postgres-best-practices`. Use
> `@supabase/ssr` (cookies), não armazene sessão em localStorage.

- [ ] **Passo 1: Instalar dependências**

Run:
```bash
bun add @supabase/ssr @supabase/supabase-js
```

- [ ] **Passo 2: Cliente de browser**

`src/lib/supabase/client.ts`:
```ts
import { createBrowserClient } from "@supabase/ssr";
import { env } from "@/lib/env";

export function createClient() {
  return createBrowserClient(env.supabaseUrl, env.supabaseAnonKey);
}
```

- [ ] **Passo 3: Cliente de server**

`src/lib/supabase/server.ts`:
```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import { env } from "@/lib/env";

export async function createClient() {
  const cookieStore = await cookies();
  return createServerClient(env.supabaseUrl, env.supabaseAnonKey, {
    cookies: {
      getAll() {
        return cookieStore.getAll();
      },
      setAll(cookiesToSet) {
        try {
          for (const { name, value, options } of cookiesToSet) {
            cookieStore.set(name, value, options);
          }
        } catch {
          // chamado de um Server Component; o middleware cuida do refresh.
        }
      },
    },
  });
}
```

- [ ] **Passo 4: Verificar typecheck**

Run: `bunx tsc --noEmit`
Expected: sem erros.

- [ ] **Passo 5: Commit**

```bash
git add src/lib/supabase/client.ts src/lib/supabase/server.ts package.json bun.lock
git commit -m "feat: clientes Supabase (browser e server) via @supabase/ssr"
```

---

### Tarefa 9: Middleware de sessão + guarda de rota

**Files:**
- Create: `src/lib/supabase/middleware.ts`, `src/middleware.ts`

- [ ] **Passo 1: Lógica do middleware**

`src/lib/supabase/middleware.ts`:
```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";
import { env } from "@/lib/env";

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request });

  const supabase = createServerClient(env.supabaseUrl, env.supabaseAnonKey, {
    cookies: {
      getAll() {
        return request.cookies.getAll();
      },
      setAll(cookiesToSet) {
        for (const { name, value } of cookiesToSet) {
          request.cookies.set(name, value);
        }
        response = NextResponse.next({ request });
        for (const { name, value, options } of cookiesToSet) {
          response.cookies.set(name, value, options);
        }
      },
    },
  });

  const {
    data: { user },
  } = await supabase.auth.getUser();

  const isAuthRoute = request.nextUrl.pathname.startsWith("/login");

  if (!user && !isAuthRoute) {
    const url = request.nextUrl.clone();
    url.pathname = "/login";
    return NextResponse.redirect(url);
  }
  if (user && isAuthRoute) {
    const url = request.nextUrl.clone();
    url.pathname = "/dashboard";
    return NextResponse.redirect(url);
  }
  return response;
}
```

- [ ] **Passo 2: Entrada do middleware**

`src/middleware.ts`:
```ts
import { type NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/middleware";

export async function middleware(request: NextRequest) {
  return updateSession(request);
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"],
};
```

- [ ] **Passo 3: Verificar typecheck**

Run: `bunx tsc --noEmit`
Expected: sem erros.

- [ ] **Passo 4: Commit**

```bash
git add src/lib/supabase/middleware.ts src/middleware.ts
git commit -m "feat: middleware de sessão Supabase + guarda de rota"
```

---

### Tarefa 10: Helper de log de auditoria

**Files:**
- Create: `src/lib/audit.ts`, `src/lib/audit.test.ts`

> História 28: toda ação importante registra autor, ação, entidade e quando. O
> `buildAuditEntry` é puro (testável); `logAudit` apenas persiste.

- [ ] **Passo 1: Escrever o teste que falha**

`src/lib/audit.test.ts`:
```ts
import { expect, test } from "bun:test";
import { buildAuditEntry } from "./audit";

test("buildAuditEntry monta a entrada com actor, ação e entidade", () => {
  const entry = buildAuditEntry({
    actorId: "user-1",
    action: "status",
    entityType: "order",
    entityId: "order-9",
    details: { from: "waiting", to: "producing" },
  });
  expect(entry).toEqual({
    actor_id: "user-1",
    action: "status",
    entity_type: "order",
    entity_id: "order-9",
    details: { from: "waiting", to: "producing" },
  });
});

test("buildAuditEntry usa details vazio por padrão", () => {
  const entry = buildAuditEntry({
    actorId: null,
    action: "create",
    entityType: "client",
    entityId: "c-1",
  });
  expect(entry.details).toEqual({});
});
```

- [ ] **Passo 2: Rodar e confirmar que falha**

Run: `bun test src/lib/audit.test.ts`
Expected: FAIL — `buildAuditEntry` não existe.

- [ ] **Passo 3: Implementar `audit.ts`**

`src/lib/audit.ts`:
```ts
import { createClient } from "@/lib/supabase/server";

export type AuditAction =
  | "create"
  | "update"
  | "delete"
  | "status"
  | "priority"
  | "stock"
  | "payment";

export type AuditInput = {
  actorId: string | null;
  action: AuditAction;
  entityType: string;
  entityId: string | null;
  details?: Record<string, unknown>;
};

export type AuditEntry = {
  actor_id: string | null;
  action: AuditAction;
  entity_type: string;
  entity_id: string | null;
  details: Record<string, unknown>;
};

export function buildAuditEntry(input: AuditInput): AuditEntry {
  return {
    actor_id: input.actorId,
    action: input.action,
    entity_type: input.entityType,
    entity_id: input.entityId,
    details: input.details ?? {},
  };
}

// Persiste a entrada. Chame de server actions após a ação principal.
export async function logAudit(input: Omit<AuditInput, "actorId">): Promise<void> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  const entry = buildAuditEntry({ ...input, actorId: user?.id ?? null });
  await supabase.from("audit_log").insert(entry);
}
```

- [ ] **Passo 4: Rodar e confirmar verde**

Run: `bun test src/lib/audit.test.ts`
Expected: PASS (2 testes).

- [ ] **Passo 5: Commit**

```bash
git add src/lib/audit.ts src/lib/audit.test.ts
git commit -m "feat: helper de log de auditoria (buildAuditEntry + logAudit)"
```

---

### Tarefa 11: Página de login/signup + server actions

**Files:**
- Create: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/login/actions.ts`

> Histórias 1–2: cadastro/login de pessoas da empresa. Sem confirmação de
> e-mail no MVP local (configurável depois). O `name` vai em `user_metadata`
> para o trigger `handle_new_user` criar o profile.

- [ ] **Passo 1: Server actions**

`src/app/(auth)/login/actions.ts`:
```ts
"use server";

import { redirect } from "next/navigation";
import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";

export async function signIn(formData: FormData) {
  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword({
    email: String(formData.get("email")),
    password: String(formData.get("password")),
  });
  if (error) redirect(`/login?error=${encodeURIComponent(error.message)}`);
  revalidatePath("/", "layout");
  redirect("/dashboard");
}

export async function signUp(formData: FormData) {
  const supabase = await createClient();
  const { error } = await supabase.auth.signUp({
    email: String(formData.get("email")),
    password: String(formData.get("password")),
    options: { data: { name: String(formData.get("name")) } },
  });
  if (error) redirect(`/login?error=${encodeURIComponent(error.message)}`);
  revalidatePath("/", "layout");
  redirect("/dashboard");
}

export async function signOut() {
  const supabase = await createClient();
  await supabase.auth.signOut();
  revalidatePath("/", "layout");
  redirect("/login");
}
```

- [ ] **Passo 2: Página de login**

`src/app/(auth)/login/page.tsx`:
```tsx
import { signIn, signUp } from "./actions";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card } from "@/components/ui/card";

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>;
}) {
  const { error } = await searchParams;
  return (
    <main className="flex min-h-dvh items-center justify-center p-4">
      <Card className="w-full max-w-sm space-y-4 p-6">
        <h1 className="text-xl font-semibold">Entrar</h1>
        {error ? <p className="text-sm text-red-600">{error}</p> : null}
        <form className="space-y-3">
          <div className="space-y-1">
            <Label htmlFor="name">Nome (apenas no cadastro)</Label>
            <Input id="name" name="name" autoComplete="name" />
          </div>
          <div className="space-y-1">
            <Label htmlFor="email">E-mail</Label>
            <Input id="email" name="email" type="email" required autoComplete="email" />
          </div>
          <div className="space-y-1">
            <Label htmlFor="password">Senha</Label>
            <Input id="password" name="password" type="password" required minLength={6} />
          </div>
          <div className="flex gap-2">
            <Button formAction={signIn} className="flex-1">
              Entrar
            </Button>
            <Button formAction={signUp} variant="outline" className="flex-1">
              Cadastrar
            </Button>
          </div>
        </form>
      </Card>
    </main>
  );
}
```

- [ ] **Passo 3: Verificar build/typecheck**

Run: `bunx tsc --noEmit && bun run build`
Expected: sem erros.

- [ ] **Passo 4: Commit**

```bash
git add "src/app/(auth)/login/page.tsx" "src/app/(auth)/login/actions.ts"
git commit -m "feat: página de login/cadastro + server actions de auth"
```

---

### Tarefa 12: App shell mobile-first + dashboard placeholder

**Files:**
- Create: `src/app/(app)/layout.tsx`, `src/app/(app)/dashboard/page.tsx`, `src/components/bottom-nav.tsx`

- [ ] **Passo 1: Bottom nav**

`src/components/bottom-nav.tsx`:
```tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";

const items = [
  { href: "/dashboard", label: "Início" },
  { href: "/estoque", label: "Estoque" },
  { href: "/pedidos", label: "Pedidos" },
  { href: "/cadastros", label: "Cadastros" },
];

export function BottomNav() {
  const pathname = usePathname();
  return (
    <nav className="fixed inset-x-0 bottom-0 z-10 grid grid-cols-4 border-t bg-background">
      {items.map((item) => {
        const active = pathname.startsWith(item.href);
        return (
          <Link
            key={item.href}
            href={item.href}
            className={`py-3 text-center text-xs ${active ? "font-semibold" : "text-muted-foreground"}`}
          >
            {item.label}
          </Link>
        );
      })}
    </nav>
  );
}
```

- [ ] **Passo 2: Layout autenticado**

`src/app/(app)/layout.tsx`:
```tsx
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { signOut } from "@/app/(auth)/login/actions";
import { BottomNav } from "@/components/bottom-nav";
import { Button } from "@/components/ui/button";

export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) redirect("/login");

  return (
    <div className="min-h-dvh pb-16">
      <header className="flex items-center justify-between border-b p-4">
        <span className="font-semibold">3D Print</span>
        <form action={signOut}>
          <Button variant="ghost" size="sm">
            Sair
          </Button>
        </form>
      </header>
      <main className="p-4">{children}</main>
      <BottomNav />
    </div>
  );
}
```

- [ ] **Passo 3: Dashboard placeholder**

`src/app/(app)/dashboard/page.tsx`:
```tsx
export default function DashboardPage() {
  return (
    <section className="space-y-2">
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        Em breve: estoque baixo, fila, produção e pagamentos pendentes.
      </p>
    </section>
  );
}
```

- [ ] **Passo 4: Verificar build**

Run: `bunx tsc --noEmit && bun run build`
Expected: sem erros.

- [ ] **Passo 5: Commit**

```bash
git add "src/app/(app)" src/components/bottom-nav.tsx
git commit -m "feat: app shell mobile-first + dashboard placeholder"
```

---

## Verificação

Prova de que a fundação funciona de verdade.

- [ ] **Suíte de testes verde**
  - Run: `bun test`
  - Expected: testes de `env` e `audit` passam.

- [ ] **Typecheck e build limpos**
  - Run: `bunx tsc --noEmit && bun run build`
  - Expected: sem erros de tipo nem de build.

- [ ] **Banco reaplica do zero**
  - Run: `bunx supabase db reset`
  - Expected: as 3 migrations aplicam sem erro; tabelas, RLS e Realtime presentes.

- [ ] **Fluxo de auth manual (`bun run dev`)**
  - Acessar `http://localhost:3000/dashboard` deslogado → redireciona para `/login`.
  - Cadastrar (nome + e-mail + senha) → cai em `/dashboard` com header e bottom nav.
  - Conferir no Supabase Studio que há linha em `auth.users` **e** em
    `public.profiles` (trigger funcionou).
  - Clicar "Sair" → volta para `/login`; acessar `/dashboard` redireciona de novo.

- [ ] **RLS efetiva**
  - No SQL Editor do Studio, como `anon`, `select * from clients` → 0 linhas/negado.
  - Autenticado, o mesmo select funciona (mesmo que vazio).

---

## Self-review (cobertura do PRD neste slice)

- Histórias 1–2 (auth/login, base de usuários) → Tarefas 9, 11, 12. ✅
- História 28 (infra de auditoria) → Tarefa 10 (tabela na 5, helper na 10). ✅
- Schema de todos os módulos (3–27, 29) → Tarefas 5–7 (tabelas + RLS + Realtime),
  consumido pelos SPECs 2–5. ✅
- CRUD de usuários (história 2) → **fora deste slice**; entra quando houver tela
  de gestão de usuários (candidato a um SPEC futuro ou anexo ao SPEC 2). Anotado
  para não ser esquecido.
